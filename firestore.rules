rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Public read for students & parents; authenticated write for teacher/admin
    match /classes/{classId}/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
